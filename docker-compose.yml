#
# Here Application
#

version: '3'
services:
  redis:
    image: "shellboot/redis"
    volumes:
      - ./var/redis:/var/redis
  mysql:
    image: "shellboot/mysql"
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./var/mysql:/var/mysql
      - ./Config/docker/mysql/conf:/usr/local/mysql/etc/conf.d
  php:
    image: "shellboot/php:redis"
    volumes:
      - .:/var/www
    depends_on:
      - mysql
      - redis
  nginx:
    image: "shellboot/nginx"
    depends_on:
     - php
    volumes:
      - .:/var/www
      - ./Config/docker/nginx/conf:/usr/local/nginx/etc
    ports:
      - "80:80"
